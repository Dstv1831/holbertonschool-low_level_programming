#include <main.h>

/**
 * print_alphabet - Function in charge of
 * printing the alphabet by
 */

void print_alphabet(void)
{
	int i;

	for (i = 97; i < 123; i++)
		_putchar(i);
	_putchar('\n');
	return (0);
}
